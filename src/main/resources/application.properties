spring.application.name=study-task

# Shared defaults for local development; production overrides them with environment variables.
app.cors.allowed-origin=${APP_CORS_ALLOWED_ORIGIN:http://localhost:5173}
app.demo.read-only=${APP_DEMO_READ_ONLY:false}
